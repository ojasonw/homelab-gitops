replicaCount: 1

image:
  repository: localstack/localstack
  tag: "4.4.0"

startServices: "s3,sqs,sns,dynamodb,lambda,kinesis,ecs,ssm,secretsmanager,iam,cloudformation"

persistence:
  enabled: true
  size: 10Gi
