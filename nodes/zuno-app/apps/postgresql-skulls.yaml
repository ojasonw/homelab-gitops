apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql-skulls-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/ojasonw/homelab-gitops.git
      targetRevision: main
      ref: valuesRepo
    - repoURL: https://github.com/ojasonw/homelab-gitops.git
      targetRevision: main
      path: infra/postgresql/chart
      helm:
        releaseName: postgresql-skulls-prod
        valueFiles:
          - $valuesRepo/infra/postgresql/skulls-prod/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: zuno-skulls
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
