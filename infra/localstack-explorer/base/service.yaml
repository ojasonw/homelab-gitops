apiVersion: v1
kind: Service
metadata:
  name: localstack-explorer
  namespace: dev
spec:
  selector:
    app: localstack-explorer
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      nodePort: 30900
  type: NodePort
