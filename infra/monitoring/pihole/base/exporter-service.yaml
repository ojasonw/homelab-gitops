apiVersion: v1
kind: Service
metadata:
  name: pihole-exporter
  namespace: monitoring
spec:
  selector:
    app: pihole-exporter
  ports:
    - name: metrics
      port: 9617
      targetPort: metrics
  type: ClusterIP
