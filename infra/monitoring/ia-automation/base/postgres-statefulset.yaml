apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ia-automation-postgres
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ia-automation-postgres
  serviceName: ia-automation-postgres
  template:
    metadata:
      labels:
        app: ia-automation-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - name: pg
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "ia_pipeline"
            - name: POSTGRES_USER
              value: "ia_worker"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ia-automation-secrets
                  key: POSTGRES_PASSWORD
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "ia_worker", "-d", "ia_pipeline"]
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: ia-automation-postgres-data
        - name: init-sql
          configMap:
            name: ia-automation-postgres-init
