apiVersion: v1
kind: Service
metadata:
  name: ia-automation-worker
  namespace: monitoring
spec:
  selector:
    app: ia-automation-worker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
