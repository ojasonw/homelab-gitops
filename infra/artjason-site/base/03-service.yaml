apiVersion: v1
kind: Service
metadata:
  name: artjason-site
  namespace: artjason-site
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: artjason-site
  ports:
    - name: http
      port: 80
      targetPort: 80
