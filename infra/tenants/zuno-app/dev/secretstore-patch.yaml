# Aplicado aos tres ExternalSecrets do base de uma vez, via seletor por kind.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nao-usado-alvo-vem-do-seletor
spec:
  secretStoreRef:
    name: infisical-zuno-app-dev
