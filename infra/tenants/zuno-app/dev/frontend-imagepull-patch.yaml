# Dev republica a mesma tag com frequencia; Always evita rodar imagem velha
# em cache do no.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  template:
    spec:
      containers:
      - name: frontend
        imagePullPolicy: Always
