# O Application separado postgresql-czanks-prod referencia este Secret por
# auth.existingSecret com o nome antigo — precisa ser preservado.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres
spec:
  target:
    name: czanks-postgres
