# Aplicado aos tres ExternalSecrets do base (secrets, registry, postgres)
# de uma vez, via seletor por kind no kustomization.yaml.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nao-usado-alvo-vem-do-seletor
spec:
  secretStoreRef:
    name: infisical-skull-prod
